import styled from "styled-components";

export const Container = styled.div`
    height: 80px;
    padding: 0 30px;
    background: var(--rocketseat);
    color: white;
  
    display: flex;
    align-items: center;
`
